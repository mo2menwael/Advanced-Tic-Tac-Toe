name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      
    - name: Cache MinGW
      id: cache-mingw
      uses: actions/cache@v4.0.2
      with:
        path: C:\tools\mingw64
        key: ${{ runner.os }}-mingw
        restore-keys: |
          ${{ runner.os }}-mingw

    - name: Install MinGW
      run: |
        choco install mingw -y
        mkdir -p C:\tools
        move C:\ProgramData\chocolatey\lib\mingw\tools\install C:\tools\mingw64
        echo "C:\tools\mingw64\bin" >> $GITHUB_PATH

    - name: Build Google Test
      if: steps.cache-mingw.outputs.cache-hit != 'true'
      run: |
        cd googletest
        mkdir build
        cd build
        cmake -G "MinGW Makefiles" ..
        mingw32-make
        cd ../..

    - name: Build using MinGW
      run: |
        g++ -Igoogletest/googletest/include -Igoogletest/googlemock/include -Lgoogletest/build/lib tests/test_tic_tac_toe.cpp -lgmock -lgtest -lgmock_main -lgtest_main -o test_tic_tac_toe.exe -pthread

    - name: Run tests
      run: .\test_tic_tac_toe.exe
